# SPDX-License-Identifier: Apache-2.0
[pytest]
testpaths = .
markers =
    simulation: requires Gazebo simulation environment
    hardware: requires physical robot hardware
    fullstack: requires full ROS2 bringup stack (Nav2, controllers)
    timeout: maximum seconds before test is aborted
addopts = --strict-markers -v
